import { RestaurantInfo } from './interfaces'
import { DishInfo } from './interfaces'

export const settings = {
    infinite: false,
    speed: 500,
    slidesToShow: 1.5,
    slidesToScroll: 1,
};





export const MaxStars: number = 5;
export const MaxPrice: number = 357;
export const MinPrice: number = 12;
export const MaxDistance: number = 4;